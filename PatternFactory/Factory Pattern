All Factory patterns encapsulate object creation either
1) By letting subclasses decide what objects to create
2) By having a factory class

Defn:-
It defines an interface for creating object, but lets subclasses decide which class to instantiate.
Factory method lets a class defer instantiation to subclasses